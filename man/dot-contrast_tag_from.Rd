% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_utils.R
\name{.contrast_tag_from}
\alias{.contrast_tag_from}
\title{Build a "\if{html}{\out{<ref>}}\emph{vs}\if{html}{\out{<test>}}" tag from DE module selection}
\usage{
.contrast_tag_from(de_sel)
}
\arguments{
\item{de_sel}{List returned by \code{mod_de_server()}, containing at least
\code{ref_level()} and \code{test_level()} reactive functions.}
}
\value{
A length-1 character string like \code{"Ref_vs_Test"} or \code{"contrast"}.
}
\description{
Uses the outputs returned by your \code{mod_de_server()} (i.e., the list with
\code{ref_level()} and \code{test_level()} reactives) to build a standardized tag for
filenames, e.g., \code{"Control_vs_Treated"}. Falls back to \code{"contrast"} if the
selection isn’t available.
}
\examples{
# Suppose de_sel is the return from mod_de_server(...)
# .contrast_tag_from(de_sel)
}
